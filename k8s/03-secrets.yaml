# ============================================
# Secrets - Credenciales de RDS
# ============================================
# IMPORTANTE: Reemplazar valores antes de aplicar
# O usar: kubectl create secret generic rds-credentials ...
apiVersion: v1
kind: Secret
metadata:
  name: rds-credentials
  namespace: notes-app
type: Opaque
stringData:
  # Reemplazar con valores reales de terraform output
  DB_HOST: "notes-app-db.ceziiqc4kof1.us-east-1.rds.amazonaws.com"
  DB_PORT: "3306"
  DB_NAME: "notes_db"
  DB_USERNAME: "notes_user"
  DB_PASSWORD: "Password123!"
  # URL completa para Spring Boot
  SPRING_DATASOURCE_URL: "jdbc:mysql://notes-app-db.ceziiqc4kof1.us-east-1.rds.amazonaws.com:3306/notes_db"
